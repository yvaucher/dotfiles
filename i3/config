# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

#exec gnome-settings-daemon

set $mod Mod4
set $mod2 Mod1

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Switch keyboard layout
#bindsym Control_L-Control_R exec setxkbmap dvp
#bindsym Alt_L-Alt_R exec setxkbmap ch fr
bindsym $mod+Ctrl+1 exec setxkbmap us dvp
bindsym $mod+Ctrl+7 exec setxkbmap ch fr

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec xrdb -merge ~/.Xresources; exec urxvt

# kill focused window
bindsym $mod+Shift+Q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+y split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
#bindcode $mod+d focus child

bindsym $mod+r exec i3-input -F 'rename workspace to %s' -P 'New name: '
bindsym $mod+a exec i3-input -F 'workspace %s' -P 'New workspace: '
#bindsym $mod+a exec i3-input -F 'workspace "%s"; exec urxvt -e bash -c "tmuxinator %s"' -P 'Open project: '
#bindsym $mod+g exec i3-input -F 'workspace "%s"; exec sleep 2; exec urxvt -e "bash"' -P 'Open project: '
bindsym $mod+g exec i3-input -F 'exec "sh /home/yvaucher/tools/open_tmux_i3_wrkspace.sh %s"' -P 'Open project: '

bindsym $mod+$mod2+j workspace prev_on_output
bindsym $mod+$mod2+k workspace next_on_output
bindsym $mod+$mod2+h workspace prev
bindsym $mod+$mod2+l workspace next

# switch to workspace
bindsym $mod+1 workspace "1: com"
bindsym $mod+2 workspace "2: work"
bindsym $mod+3 workspace "3: music"
bindsym $mod+4 workspace "4: trello"
bindsym $mod+5 workspace "5: dev"
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+plus move workspace "1: com"
bindsym $mod+Shift+quotedbl move workspace "2: work"
bindsym $mod+Shift+asterisk move workspace "3: music"
bindsym $mod+Shift+ccedilla move workspace "4: trello"
bindsym $mod+Shift+percent move workspace "5: dev"
bindsym $mod+Shift+ampersand move workspace 6
bindsym $mod+Shift+slash move workspace 7
bindsym $mod+Shift+parenleft move workspace 8
bindsym $mod+Shift+parenright move workspace 9
bindsym $mod+Shift+equal move workspace 10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the window is resized so that it has
        # more space on its left

bindsym j resize shrink left 10 px or 10 ppt
bindsym Shift+J resize grow   left 10 px or 10 ppt

bindsym k resize shrink down 10 px or 10 ppt
bindsym Shift+K resize grow   down 10 px or 10 ppt

bindsym l resize shrink up 10 px or 10 ppt
bindsym Shift+L resize grow   up 10 px or 10 ppt

bindsym eacute resize shrink right 10 px or 10 ppt
bindsym Shift+odiaeresis resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
bindsym Left resize shrink left 10 px or 10 ppt
bindsym Shift+Left resize grow   left 10 px or 10 ppt

bindsym Down resize shrink down 10 px or 10 ppt
bindsym Shift+Down resize grow   down 10 px or 10 ppt

bindsym Up resize shrink up 10 px or 10 ppt
bindsym Shift+Up resize grow   up 10 px or 10 ppt

bindsym Right resize shrink right 10 px or 10 ppt
bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
bindsym Return mode "default"
bindsym Escape mode "default"
}

bindsym $mod+Ctrl+r mode "resize"

for_window [class="Gnome-control-center"] floating enable
for_window [class="Gnome-keyring-prompt-3"] floating enable
for_window [class="gcalctool"] floating enable

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command /home/yvaucher/.i3/i3script.sh
        #status_command i3status
}

# gnome helpers
exec --no-startup-id gnome-settings-daemon &
exec --no-startup-id nm-applet &

# scratchpad
bindsym $mod+x scratchpad show
bindsym $mod+Shift+x move scratchpad

bindsym $mod+Shift+Y exec i3lock -d -i /home/yvaucher/Pictures/Wallpapers/sunrise-from-space-1680x1050.png -c 000000 -d

# remap caps lock to ctrl
exec setxkbmap -option 'ctrl:nocaps'
# make sure urxvt colors are loaded
exec xrdb -merge ~/.Xresources
# load wallpaper
exec sh ~/.fehbg &


# define applications
set $webmail "google-chrome --new-window --profile-directory=Default https://mail.google.com/mail/ca"
set $chatter xchat
set $twitter "google-chrome --new-window --profile-directory=\'Yannick\' http://twitter.com"
set $time_tracker hamster-time-tracker
set $player rhythmbox

# Startup config
# utils on left screen
workspace "1: com" output DP-2
workspace "2: work" output DP-2
workspace "3: music" output DP-2
workspace "4: trello" output DP-2


# projects on right screen
workspace "5: dev" output DP-3

# assign application to workspaces
assign [class="Xchat"] "1: com"
assign [class="Hamster"] "2: work"
assign [class="Rhythmbox"] "3: music"

# Launch applications
exec $chatter
exec --no-startup-id i3-msg 'workspace "1: com"; exec $webmail;'
exec $time_tracker
exec $player
